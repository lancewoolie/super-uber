function loadMoney() {
    const container = document.getElementById('money-dashboard');
    container.innerHTML = '<p>Mileage & earnings tracker (Uber API connect coming).</p>';
    // TODO: Manual input form + Uber OAuth
    console.log('Money loaded (stub)');
}
