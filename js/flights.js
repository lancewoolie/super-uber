function loadFlights() {
    const container = document.getElementById('flights-list');
    container.innerHTML = '<p>Flights data for BTR/MSY airports will load here.</p>';
    // TODO: fetch AviationStack API
    console.log('Flights loaded (stub)');
}
